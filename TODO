Player Schedulers!  They cancel automatically if the player goes offline.
    player.getScheduler().runDelayed(plugin, task -> { if (maxHealthAttribute != null) maxHealthAttribute.setBaseValue(20); }, null, 1200);

Add separate recipes for the augmented and regular versions of each effect
- For convenience, add a quick way to keep two recipes the same (maybe define recipe then state applicable effects for that recipe)

Add a config to make villagers immune to abilities
Update the resource pack to make the bow animation faster (maybe use cmd for speed users specifically?)

Infuse Revamp Revamp
- Armor has animations to show durability
- hearts show icons above the player instead of numbers
